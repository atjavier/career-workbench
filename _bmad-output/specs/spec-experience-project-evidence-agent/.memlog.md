---
topic: Experience and Projects source-folder resume evidence agent
updated: 2026-08-26T19:29
---

- (direction) Supersedes active Markdown-folder collection imports: Projects and Experiences begin with a user-selected actual project or experience source folder containing code and related files.
- (capability) CAP-1: The user can explicitly select a Project or Experience source folder for resume documentation; success is that the active UI neither restricts the source to Markdown nor treats raw source files as direct evidence.
- (capability) CAP-2: A dedicated resume-evidence documentation skill inspects a selected source folder and produces review-only Markdown artifacts; success is that output is bounded, traceable, and requires explicit import plus individual evidence approval before claim use.
- (constraint) The documentation workflow must be local, explicit, bounded, read-only for the selected source folder, reject links/reparse points and unsafe or unsupported inputs, and never use network, background watching, or automatic evidence approval.
- (constraint) The source-folder inspection contract must preserve selected-folder-relative provenance and explicit unknowns; it may not manufacture metrics, ownership, dates, users, outcomes, deployment status, or skills.
- (constraint) Resume.pdf remains the immutable Resume Edit template and is not an Experience or Projects import or extraction source.
- (question) The product runtime authority for executing the documentation skill is unresolved: decide whether this remains a user-invoked Codex workflow or gains a separately specified application-native local agent boundary.
- (event) Coherence validation passed: CAP-1 through CAP-3 each have intent and testable success; constraints, non-goals, and a concrete success signal are present.
- (event) Preservation validation passed: every user direction landed in SPEC.md or documentation-contract.md; wrapper-only content omitted.
- (event) Spec finalized 2026-08-26 with runtime authority retained as an open question.
- (direction) The resume evidence documenter is an application-native skill executed by the configured local LLM, not a Codex skill or external handoff; the UI must not expose a copyable Codex prompt.
- (constraint) The application agent must have an allowlisted skill registry, typed inputs/outputs, explicit user consent, bounded local file reads, no shell/network/cloud/background work, and no writes outside designated artifact and review stores.
- (constraint) Each agent skill declares its permitted capabilities, required inputs, readable file extensions/directories, writable paths, size/depth/count limits, and prohibited operations; the runtime rejects undeclared tool or file access.
- (capability) CAP-4: The local LLM can document a user-selected Project or Experience source folder through an application-native resume-evidence skill; success is three review-only artifacts produced under the declared output boundary with source provenance and no Codex handoff.
- (capability) CAP-5: The Experience & Projects UI lets the user select a source folder, see scoped consent and progress state, and review/import generated artifacts without exposing prompts, raw paths, private diagnostics, or unapproved claims.
